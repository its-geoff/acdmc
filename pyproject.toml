[project]
name = "acdmc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.16.4",
]

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [".github"]

[tool.ruff.lint]
select = ["E", "F", "B", "I"]